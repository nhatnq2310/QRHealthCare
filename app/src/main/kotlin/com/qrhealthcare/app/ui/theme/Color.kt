package com.qrhealthcare.app.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Primary (Red — matches web app's --primary: 0 84% 50%) ─────────────────
val Red500   = Color(0xFFE53935)  // Primary red
val Red600   = Color(0xFFD32F2F)  // Darker red (pressed state)
val Red50    = Color(0xFFFFEBEE)  // Very light red (backgrounds)
val RedAlpha = Color(0x1AE53935)  // 10% red (subtle highlights)

// ─── Neutral ─────────────────────────────────────────────────────────────────
val White    = Color(0xFFFFFFFF)
val Black    = Color(0xFF171717)  // --foreground: 0 0% 9%
val Gray50   = Color(0xFFF5F5F5)  // --secondary: 0 0% 96%
val Gray100  = Color(0xFFE5E5E5)  // --border: 0 0% 90%
val Gray400  = Color(0xFF737373)  // --muted-foreground: 0 0% 45%
val Gray700  = Color(0xFF404040)  // Subtitle text
val Gray900  = Color(0xFF171717)  // Same as Black

// ─── Status colors ───────────────────────────────────────────────────────────
val SuccessGreen = Color(0xFF4CAF50)
val WarningAmber = Color(0xFFFFC107)
val InfoBlue     = Color(0xFF2196F3)
